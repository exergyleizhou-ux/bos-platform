# BOS Pipeline v9.0 Python dependencies
#
# Phase B B1: numpy 2.x migration + causal libs
# See _reports/PHASE_B_B0_5_NUMPY_AUDIT.md for audit
# See _reports/PHASE_B_B1_COMPLETION.md for landing report

# Web framework
fastapi==0.109.2
starlette==0.36.3
uvicorn[standard]==0.27.1
uvloop==0.19.0; platform_system != "Windows"
httptools==0.6.1
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
greenlet==3.0.3
aiosqlite>=0.22         # test SQLite async driver (B0.5 F1)

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation
pydantic[email]>=2.6,<3.0  # B0.5 F5: bumped from 2.6.1 (langchain-core pulls 2.13+)
# pydantic-core pin dropped — let pydantic resolve a matching version
pydantic-settings==2.1.0
email-validator==2.1.0

# Redis and Celery
redis[hiredis]==5.0.1
celery[redis]==5.3.6

# HTTP client
httpx==0.27.0

# Scientific computing
numpy>=2.0,<3.0         # B0.5 audit validated against 2.4.5
scipy>=1.13             # B0.5 F3: bumped from 1.12.0 (numpy 2.x binary compat)

# Data export
openpyxl==3.1.2
pyarrow>=16             # B0.5 F2: bumped from 15.0.0 (numpy 2.x C-API)

# Causal inference (Phase B)
dowhy==0.14             # 4-step pipeline: model / identify / estimate / refute
econml==0.16.0          # DML estimators + DML mediation (D9 = α, D14 = γ)

# LLM / Agent (Phase A inlined per B0.5 F4 — was agent/requirements.txt only)
langchain-core>=0.3
langgraph>=0.2
langchain-anthropic>=0.3
anthropic>=0.40

# Logging and monitoring
structlog==24.1.0
python-json-logger==2.0.7

# Testing
pytest==8.0.1
pytest-asyncio==0.23.5
pytest-cov==4.1.0
factory-boy==3.3.0
Pillow==10.2.0

# Code quality
black==24.1.1
ruff==0.2.1
mypy==1.8.0

# ─────────────────────────────────────────────────────────────────
# Phase C C1 — Bayesian baseline (added per PHASE_C_C1_DESIGN.md §10)
# numpy 2.x compatible; pytensor uses Python fallback if g++ absent.
# Smoke test on synthetic ATE=2.0 recovers posterior mean 1.847,
# 95% HDI [1.551, 2.116], r_hat 1.0000, ESS 803, evidence='validated'.
# ─────────────────────────────────────────────────────────────────
pymc>=5.10,<6.0
arviz>=0.16,<1.0
