# BOS Agent — Phase A runtime dependencies.
# NOT installed in Phase 0.5. Install only when Phase A begins.

langgraph>=0.2
langchain-core>=0.3
httpx>=0.27
pydantic>=2.6
fastapi>=0.109
uvicorn[standard]>=0.27
