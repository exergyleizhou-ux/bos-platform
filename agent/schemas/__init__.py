"""Agent schema mirror root.

Mirrors ``backend/app/schemas/causal/`` modulo paths. Parity is
enforced by ``agent/tests/test_schema_parity.py`` (compares
``model_json_schema()`` between mirror and source).
"""
