"""Mirror of ``backend/app/schemas/causal/``.

Six modules: ``common`` + the five endpoint schemas (``identify``,
``estimate``, ``refute``, ``mediation``, ``sensitivity``). The
``SCHEMA_VERSION`` constants mirror the backend values (B.1 - B.5)
verbatim; parity is verified at test time.
"""
