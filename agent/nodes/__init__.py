"""Agent node implementations.

Step 2 ship: empty package (directory structure ready). Step 3-5
will add ``router_node``, ``causal_{identify,estimate,refute,
mediation,sensitivity}_node``, and ``render`` modules per
``_reports/PHASE_B_B4_DESIGN.md`` §4.
"""
